apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: clawswarm
  labels:
    app: clawswarm
    component: postgres
spec:
  selector:
    app: clawswarm
    component: postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  type: ClusterIP
